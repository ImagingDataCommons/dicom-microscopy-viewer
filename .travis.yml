language: node_js
node_js:
  - node

sudo: false

before_install:
  - corepack enable
  - corepack prepare pnpm@10.34.1 --activate

script:
  - pnpm install --frozen-lockfile
  - pnpm run build
  - pnpm run test
